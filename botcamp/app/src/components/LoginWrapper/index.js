import styled from 'styled-components';

import backgorund from "../../img/ecossistema-background.png"

const LoginWrapper = styled.main `
    width: 375px;
    height: 667px;
    background-image: url(${backgorund});
`;


export default LoginWrapper;